Google login public-config fix

Upload these files to GitHub in these exact locations:

- App.tsx
- App.txt
- api/_supabase.js
- api/public-config.js

Important:

- api/public-config.js must be inside the top-level api folder.
- It should NOT be inside dist, src, backend, outputs, or another nested api folder.
- After GitHub has these files, redeploy Vercel with build cache off.

Then test:

https://www.intuisity.com/api/public-config

Expected result:

{"googleClientId":"your-google-client-id.apps.googleusercontent.com"}

If /api/health is also 404, Vercel is not deploying the api folder at all. Check that the Vercel project root directory is blank or points to the folder containing package.json, vercel.json, App.tsx, and api.
